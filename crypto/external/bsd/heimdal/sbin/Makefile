# $NetBSD$

.include "bsd.own.mk"

.if (${MKKERBEROS} != "no")
SUBDIR =		\
	hprop		\
	iprop-log	\
	kadmin		\
	kcm		\
	kdc		\
	kdigest		\
	kimpersonate	\
	kstash		\
	ktutil

.endif	# MKKERBEROS != no

.include <bsd.subdir.mk>
