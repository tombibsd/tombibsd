# $NetBSD$

.include "bsd.own.mk"

.if (${MKKERBEROS} != "no")

SUBDIR =		\
	digest-service	\
	hpropd		\
	ipropd-master	\
	ipropd-slave	\
	kadmind		\
	kpasswdd

.endif	# MKKERBEROS != no

.include <bsd.subdir.mk>
